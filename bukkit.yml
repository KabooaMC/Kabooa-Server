settings:
    update-folder: update
    spawn-radius: 0
    permissions-file: permissions.yml
database:
    password: kabooa
    driver: org.sqlite.JDBC
    isolation: SERIALIZABLE
    url: jdbc:sqlite:{DIR}{NAME}.db
    username: kabooabunga
aliases:
    icanhasbukkit:
    - version
